export default (existingItems, newItems) => [...existingItems, ...newItems];

// const playlist = state.list.tracks.find(p => p.id === state.id);
//       if (playlist.tracks.items) tracks.items.unshift(...playlist.tracks.items);
//       playlist.tracks = tracks;
